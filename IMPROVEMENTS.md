# 🗺 نقشهٔ راه بهبود کیفیت موتورها

> مستند به کد فعلی (کامیت `b266827`) — هر آیتم با «اثر × هزینه» و محل دقیق اجرا.
> اولویت: 🟥 پراثر فوری · 🟧 میان‌مدت · 🟨 آراستگی و بلندمدت

---

## ۱) هستهٔ استخراج پرده (pitch) — پایهٔ همه‌چیز

وضعیت فعلی: Praat `to_pitch_ac` با `very_accurate=True`، پنجره‌ای برای فایل بلند،
پاک‌سازی جهش اکتاوی (۵۵۰¢)، میانه‌گیر ۵تایی. صحت عالی در صدای تمیز؛ ضعف اصلی
در **صدای نویزی/میکروفون** و خطاهای اکتاوی.

| # | پیشنهاد | شرح فنی | اثر | هزینه |
|---|---|---|---|---|
| ۱.۱ | 🟥 **قطع پردازش مرورگر صدا** | در `getUserMedia` از `{echoCancellation:false, noiseSuppression:false, autoGainControl:false}` استفاده شود — AGC/EC مرورگر نت پیوسته را می‌شکند و پرده را جا می‌اندازد. **بزرگ‌ترین ارتقای زنده با یک خط کد.** | 🔥 بالا | ۱۵ دقیقه |
| ۱.۲ | 🟥 **انسمبل PYIN** | `librosa.pyin` موازی با Praat؛ رأی‌گیری وزنی (Praat 0.7 / pyin 0.3). pyin در صدای نویزگیر به‌مراتب کمتر اکتا غلط می‌زند. | بالا | ۲–۳ ساعت |
| ۱.۳ | 🟧 **Viterbi اکتاوی** | به‌جای پاک‌سازی موضعی جهش‌ها، مسیر اکتاوی با HMM روی شبکهٔ درجات مقام انتخاب شود (جهش درست از نادرست تفکیک می‌شود). | بالا | ۴–۶ ساعت |
| ۱.۴ | 🟧 **fmax داینامیک** | رنج اولیه از ۵ ثانیهٔ اول → fmax شخصی (زن/مرد). خطای اکتاوی بالا در صداهای بم کاهش می‌یابد. | متوسط | ۱ ساعت |
| ۱.۵ | 🟨 `time_step=0.005` برای فایل‌های <۳۰ ثانیه | دقت ویبرو/گلیساندو دوبرابر. | متوسط | ۳۰ دقیقه |

## ۲) تشخیص مقام و تونیک

وضعیت فعلی: هیستوگرام ۲۴۰-بینی (۵¢) + بهاتاچاریای دوزیسته + قرار-بونوس + پالایش
تونیک روی شبکهٔ ۵۰¢ + اولویت پروفایل صوتی. Top-3 شبیه‌سازی ~۹۳٪.

| # | پیشنهاد | شرح فنی | اثر | هزینه |
|---|---|---|---|---|
| ۲.۱ | 🟥 **تست‌بانچ واقعی برچسب‌دار** | ۳۰–۵۰ تلاوت کوتاه با مقام شناخته‌شده (از استادهای معروف) + اسکریپت سنجهٔ top-1/top-3. هر تغییر آینده با عدد سنجیده می‌شود، نه حدس. | بالا (پایهٔ علمی بقیه) | ۳–۴ ساعت |
| ۲.۲ | 🟥 **امتیاز گذر (sayr-aware)** | به امتیاز BH، درست‌نمایی گذرها (از ماتریس پروفایل‌های سبک موجود) اضافه شود: `score = 0.7·BH + 0.3·loglik(transitions)`. تمایز مقامات هم‌گام (بیاتی/کرد، رست/عجم) که هیستوگرام به‌تنهایی گیر می‌کند. | بالا | ۳ ساعت |
| ۲.۳ | 🟧 **کالیبراسیون اطمینان** | نگاشت نمره→درصد واقعی با منحنی سیگموئیدی کالیبره‌شده روی تست‌بانچ؛ «۸۵٪» دیگر یعنی واقعاً ۸۵٪. | متوسط | ۲ ساعت |
| ۲.۴ | 🟧 **تشخیص مدولاسیون دقیق** | `maqam_timeline` فعلی پنجره‌ای است؛ با تغییرنما (change-point) لحظهٔ تغییر مقام + مقام قبل/بعد + درجهٔ پل گزارش شود. | متوسط | ۴ ساعت |
| ۲.۵ | 🟨 تونیک پیوسته | پالایش با جستجوی بخش‌طلایی ناحیه‌ای (نه شبکهٔ ۵۰¢) — خطای تونیک <۱۰¢. | کم | ۱ ساعت |
| ۲.۶ | 🟨 ریزمقام‌ها | بیاتی‌حسینی، راست-سوزناک، حجاز-کار… به‌عنوان `jins_alternatives` دومرحله‌ای در جدول + UI. | کم | ۳ ساعت |

## ۳) تفکیک نت و زینت‌ها (segment_notes)

| # | پیشنهاد | شرح | اثر | هزینه |
|---|---|---|---|---|
| ۳.۱ | 🟥 **برچسب گلیساندو** | نت‌های پایدار نیست ولی روند مونوتون دارند = «سُر» — جدا برچسب بخورند (الان یا تکه‌تکه یا حذف می‌شوند). در ملوگراف هم خط مورب رسم شود. | متوسط–بالا | ۲ ساعت |
| ۳.۲ | 🟧 **زینت‌سنجی** | طبقه‌بندی خودکار: ویبرو (داریم)، ترِمولو دامنه‌ای، مورِد/گردونه (نت‌های ۸۰–۱۲۰ms متوالی)، سکوت زینتی. به سبک‌شناسی و امتیاز feed می‌شود. | متوسط | ۴ ساعت |
| ۳.۳ | 🟨 پرش‌نگار ماوال | حالت «نُت‌ریز» برای مرصاد/موال با `stability_cents` آزاد. | کم | ۱ ساعت |

## ۴) امتیازدهی تمرین (practice_scorer)

| # | پیشنهاد | شرح | اثر | هزینه |
|---|---|---|---|---|
| ۴.۱ | 🟥 **فیدبک فراز-به-فراز قابل‌پخش** | از `phrase_breakdown` — لیست فرازها با نمره؛ کلیک → پخش همان ثانیه + «اینجا از مقام خارج شدی (نوا −۳۰¢)». بزرگ‌ترین پرش کیفی احساس کاربر. | بالا | ۳ ساعت |
| ۴.۲ | 🟧 امتیاز زینت (ویبرو) | نرخ ۵–۷Hz و دامنهٔ ۳۰–۶۰¢ مطلوب؛ زیرامتیاز پنجم وزن ۰٫۱. | متوسط | ۲ ساعت |
| ۴.۳ | 🟧 نفس‌گیری و جمله‌بندی | طول میانگین فراز + نسبت مکث به‌جا/بیش‌مکث. | متوسط | ۲ ساعت |
| ۴.۴ | 🟨 منحنی امتیاز کالیبره | نرّم‌سازی سیگموئیدی به‌جای clamp — ۹۷ واقعاً دست‌نیافتنی‌تر از ۹۰. | کم | ۱ ساعت |
| ۴.۵ | 🟨 اهداف چالشی | «قرار ۱۰۰٪»، «۸۰٪ تا غماز برو» — چالش‌های انتخابی با XP بیشتر. | کم | ۲ ساعت |

## ۵) موتور ملودیک

| # | پیشنهاد | شرح | اثر | هزینه |
|---|---|---|---|---|
| ۵.۱ | 🟥 **ریتم واقعی از خود کاربر** | الگوی IOI فراز کاربر (نرمال‌شده به میانه) بازسازی شود، نه نوسان حول میانه — پیشنهاد «مالِ خودش» می‌شود. | بالا | ۲–۳ ساعت |
| ۵.۲ | 🟧 جملهٔ پرسش–پاسخ | واریانت ۱ روی غماز تمام شود، واریانت ۲ روی قرار؛ مکث بین دو جمله. ساختار موسیقایی واقعی تلاوت. | متوسط–بالا | ۳ ساعت |
| ۵.۳ | 🟧 ممنوعه‌های مقامی از داده | گذرهای با فراوانی صفر در ۳ پروفایل سبک موجود (و آینده) → جریمه در `_gen_path`/`_style_path`. | متوسط | ۲ ساعت |
| ۵.۴ | 🟧 پخش زینت‌دار | روی نت‌های ≥۰٫۶s یک LFO ویبرو (۵٫۵Hz، ±۳۵¢) در Web Audio — پیانو به «هِی» نزدیک‌تر شنیده می‌شود. | متوسط | ۲ ساعت |
| ۵.۵ | 🟨 بازگیری موتیف با DTW | به‌جای رایج‌ترین، موتیفِ نزدیک‌تر به جایگاه فعلی انتخاب شود. | کم | ۲ ساعت |
| ۵.۶ | 🟨 ترانه‌ریز (پیشنهاد قبلی) | ورود متن آیه — روی هجاها بنشاند (پروژهٔ بزرگ‌تر). | بالا | ۱–۲ روز |

## ۶) یادگیری سبک (style_learner)

| # | پیشنهاد | شرح | اثر | هزینه |
|---|---|---|---|---|
| ۶.۱ | 🟥 **هموارسازی ماتریس گذر** | Laplace/مخلوط یکنواخت (α=0.5) — گذر صفر مطلق حذف نشود؛ پیشنهادهای سبک تنوع پیدا کنند و شباهت منصفانه شود. | بالا | ۱ ساعت |
| ۶.۲ | 🟧 گذر دومرحله‌ای | `a→b→c` برتر هر قاری (۳–۵ الگوی امضایی) — در کارت سبک نمایش + در تولید. | متوسط | ۲ ساعت |
| ۶.۳ | 🟧 کشف موتیف امضایی | الگوریتم کشف الگوی تکرارشونده (SIA ساده روی توالی پله‌ها) — «فرمول منشاوی». | متوسط | ۳–۴ ساعت |
| ۶.۴ | 🟨 اعتبارسنجی درون‌پروفایل | leave-one-out تمایز (مثل تستی که ۳/۳ شد) داخل خود پروفایل ذخیره شود → «قدرت این پروفایل: ۸۷٪». | کم | ۱ ساعت |
| ۶.۵ | 🟨 نقشهٔ سبک | اسکتر دوبعدی همهٔ پروفایل‌ها (MDS روی فاصلهٔ کسینوسی) — «صدای تو بین منشاوی و مصطفی کجاست؟» | کم | ۲–۳ ساعت |

## ۷) مربی زنده (WS)

| # | پیشنهاد | شرح | اثر | هزینه |
|---|---|---|---|---|
| ۷.۱ | 🟥 سنجش تأخیر و chunk ۲۰۴۸ | اگر RTT خوب بود، اندازهٔ بفرست ۴۰۹۶→۲۰۴۸ (دقت کوک زنده دوبرابر). | متوسط | ۱–۲ ساعت |
| ۷.۲ | 🟧 زمان‌بندی هینت | هینت فقط بعد از ۱٫۵s انحراف پایدار — نویز بصری حذف؛ «نگه دار ✓» فقط یک‌بار. | متوسط | ۱ ساعت |
| ۷.۳ | 🟨 امتیاز زندهٔ فراز جاری | همان امتیازدهی مینی روی بافر فراز — نمایش زیر کارت مربی. | کم | ۲ ساعت |

## ۸) مقایسهٔ دو فراز

| # | پیشنهاد | شرح | اثر | هزینه |
|---|---|---|---|---|
| ۸.۱ | 🟧 نوار ساکو-چیبا | محدودسازی مسیر DTW (band ±۲۰٪) — سریع‌تر و تطابق منطقی‌تر در فرازهای با تکرار. | متوسط | ۱–۲ ساعت |
| ۸.۲ | 🟨 نقشهٔ حرارت اختلاف | روی موج رنگی، نقاط بیشترین انحراف نت‌به‌نت نشان داده شود. | کم | ۲ ساعت |

## ۹) وب و تجربهٔ کاربری

| # | پیشنهاد | شرح | اثر | هزینه |
|---|---|---|---|---|
| ۹.۱ | 🟥 **جداسازی JS** | `index.html` = ۲۴۳۶ خط با اسکریپت درون‌خطی → `web/app.js` جدا + minify نکن (خوانا). نگهداری و تست‌پذیری. | بالا (بلندمدت) | ۳ ساعت |
| ۹.۲ | 🟧 کلیدهای پیانو کلیک‌پذیر | در هر دو پیانو (اصلی/موتور) کلیک = پخش همان درجه — تمرین شنیداری. | متوسط | ۱ ساعت |
| ۹.۳ | 🟨 Service Worker آفلاین | شِل UI کش شود؛ تحلیل‌ها همیشه به سرور. | کم | ۲ ساعت |
| ۹.۴ | 🟨 خروجی CSV تاریخچه | دکمهٔ دانلود `practice_history` + پاک‌سازی انتخابی. | کم | ۱ ساعت |

## ۱۰) زیرساخت و کیفیت نرم‌افزار

| # | پیشنهاد | شرح | اثر | هزینه |
|---|---|---|---|---|
| ۱۰.۱ | 🟥 **پاک‌سازی خودکار jobs** | الان ~۳۰ پوشهٔ job با صدا روی دیسک مانده. TTL ۲۴ ساعت + پاکسازی در startup. | بالا | ۱ ساعت |
| ۱۰.۲ | 🟥 تست‌های pytest | melody_engine (قطعیت seed)، scorer (۳ سناریو)، refine_prior، style_sim — + GitHub Actions CI. | بالا | ۳ ساعت |
| ۱۰.۳ | 🟧 نسخه‌ثابت وابستگی‌ها | `requirements.txt` با `==` پین شود (librosa و numpy آسیب‌پذیرترین‌ها). | متوسط | ۳۰ دقیقه |
| ۱۰.۴ | 🟧 ماندگاری JOBS روی دیسک | ری‌استارت سرور، گزارش‌های done را از `report.json` بازخوانی کند (فایل‌ها از قبل هست!). | متوسط | ۱–۲ ساعت |
| ۱۰.۵ | 🟨 لاگ ساخت‌یافته | خطاها به‌جای stdout پراکنده → فایل لاگ JSON با job_id. | کم | ۱ ساعت |
| ۱۰.۶ | 🟨 Healthcheck داکر | `/api/styles` سبک است؛ endpoint `/api/health` + healthcheck در compose. | کم | ۳۰ دقیقه |

---

## 🎯 جادهٔ پیشنهادی (ترتیب اجرا)

**فاز ۱ — پرتاب موشک (یک نشست):**
۱.۱ قطع پردازش مرورگر · ۱۰.۱ پاک‌سازی jobs · ۶.۱ هموارسازی سبک · ۱۰.۳ پین نسخه‌ها

**فاز ۲ — دقت علمی:**
۲.۱ تست‌بانچ واقعی · ۲.۲ امتیاز گذر · ۱۰.۲ تست‌ها+CI · ۴.۱ فیدبک فرازی قابل‌پخش

**فاز ۳ — عمق موسیقایی:**
۵.۱ ریتم واقعی · ۵.۲ پرسش–پاسخ · ۳.۱ گلیساندو · ۴.۲ ویبرو · ۶.۲/۶.۳ الگوهای امضایی

**فاز ۴ — آراستگی:**
۹.۱ جداسازی JS · بقیهٔ 🟨ها

---
*هر آیتم مستقل است؛ گفتن شمارهٔ آیتم (مثلاً «۱.۱ و ۴.۱») برای اجرا کافی است.*
