# راهنمای استقرار روی سرور واقعی (Production Deployment)

این راهنما مرحله‌به‌مرحله توضیح می‌دهد چطور این پروژه را روی یک سرور
(VPS) که از یک شرکت میزبانی خریده‌اید، با دامنه و HTTPS بالا بیاورید.

---

## ۱) چه سروری بخرم؟

برای «عموم مردم، ترافیک متوسط» که گفتید، حداقل‌های پیشنهادی:

| مشخصه | حداقل | پیشنهادی |
|---|---|---|
| CPU | ۲ هسته | ۴ هسته |
| RAM | ۴ گیگابایت | ۸ گیگابایت |
| دیسک | ۴۰ گیگابایت SSD | ۸۰ گیگابایت SSD |
| سیستم‌عامل | Ubuntu 22.04 / 24.04 LTS (۶۴ بیتی) | همین |

**چرا این مقدار؟** تحلیل صدا (به‌خصوص Praat pitch-extraction و رسم
ملوگراف با matplotlib) کاملاً CPU-bound است و برای فایل‌های طولانی چند
ده ثانیه تا چند دقیقه طول می‌کشد. RAM بیشتر یعنی چند کاربر هم‌زمان
می‌توانند بدون کندی سیستم تحلیل کنند. اگر بعداً ترافیک بیشتر شد، ارتقا
دادن (resize) سرور در اکثر شرکت‌های میزبانی چند دقیقه‌ای انجام می‌شود.

هر شرکتی که VPS اوبونتو با IP عمومی بدهد کافی است (مثلاً Hetzner،
DigitalOcean، OVH، Vultr، Arvan Cloud/ابر آروان یا سرورهای داخلی ایرانی
مشابه). چیز خاصی لازم نیست، فقط:
- دسترسی SSH (root یا کاربر با sudo)
- IP عمومی ثابت (Static/Public IP)

---

## ۲) قبل از رفتن روی سرور: تنظیم دامنه

چون گفتید دامنه دارید و SSL هم می‌خواهید:

1. وارد پنل مدیریت DNS دامنه‌تان شوید (جایی که دامنه را خریده‌اید).
2. یک رکورد **A** بسازید:
   - Host/Name: `@` (یا زیردامنه‌ای مثل `quran`)
   - Value: **IP عمومی سرور** (بعد از خرید سرور از شرکت میزبانی به شما داده می‌شود)
   - TTL: پیش‌فرض کافی است
3. صبر کنید تا DNS propagate شود (معمولاً چند دقیقه تا چند ساعت). با
   دستور زیر از روی همان سرور می‌توانید چک کنید که دامنه به IP درست
   اشاره می‌کند:
   ```bash
   dig +short yourdomain.com
   ```
   خروجی باید همان IP سرور باشد.

⚠️ **مهم:** گواهی SSL را نمی‌توان گرفت مگر این‌که این مرحله کامل شده
باشد — Let's Encrypt باید بتواند از طریق دامنه به سرور شما وصل شود.

---

## ۳) اتصال به سرور و نصب پیش‌نیازها

```bash
ssh root@YOUR_SERVER_IP
```

نصب Docker و Docker Compose (روی Ubuntu):

```bash
curl -fsSL https://get.docker.com | sh
apt-get install -y docker-compose-plugin
```

بررسی نصب:
```bash
docker --version
docker compose version
```

---

## ۴) انتقال کد پروژه به سرور

ساده‌ترین راه، کلون کردن مستقیم از گیت‌هاب روی خود سرور:

```bash
cd /opt
git clone https://github.com/mahdi-talebian/Ai.git quran-analyzer
cd quran-analyzer
```

(اگر مخزن private است، باید یک Personal Access Token هنگام کلون وارد
کنید یا SSH deploy key تنظیم کنید.)

---

## ۵) تنظیم متغیرهای محیطی

```bash
cp .env.example .env
nano .env
```

مقادیر را با اطلاعات واقعی خودتان پر کنید:
```
DOMAIN=yourdomain.com
EMAIL=you@example.com
```

---

## ۶) دریافت گواهی SSL (فقط بار اول)

```bash
chmod +x deploy/init-ssl.sh
./deploy/init-ssl.sh
```

این اسکریپت به‌صورت خودکار:
1. یک nginx موقت (فقط HTTP) بالا می‌آورد تا Let's Encrypt بتواند دامنه
   را تأیید کند.
2. گواهی SSL رایگان می‌گیرد.
3. کل سیستم (اپ + nginx با HTTPS واقعی) را بالا می‌آورد.

اگر با خطا مواجه شدید، محتمل‌ترین علت این است که DNS هنوز به‌روز نشده —
دوباره با `dig +short yourdomain.com` چک کنید.

---

## ۷) بررسی نهایی

مرورگر را باز کنید و به آدرس زیر بروید:
```
https://yourdomain.com
```

باید صفحهٔ وب پروژه (تب‌های آپلود، مقایسه، میکروفون زنده) را ببینید،
با قفل سبز HTTPS در نوار آدرس.

بررسی وضعیت سرویس‌ها:
```bash
docker compose ps
docker compose logs -f app        # لاگ زندهٔ اپ پایتون
docker compose logs -f nginx      # لاگ زندهٔ nginx
```

---

## ۸) به‌روزرسانی بعدی کد (وقتی تغییری در گیت‌هاب push شد)

```bash
cd /opt/quran-analyzer
git pull
docker compose up -d --build app
```

(فقط سرویس `app` نیاز به rebuild دارد؛ nginx و certbot تغییری نکرده‌اند.)

---

## ۹) پشتیبان‌گیری از نتایج تحلیل (اختیاری)

نتایج هر job (گزارش JSON، ملوگراف PNG، ویدیوی پیانو) در یک Docker
volume به نام `jobs_data` نگه‌داری می‌شود — با ری‌استارت/rebuild از بین
نمی‌رود. برای گرفتن نسخهٔ پشتیبان:

```bash
docker run --rm -v quran-analyzer_jobs_data:/data -v $(pwd):/backup \
    alpine tar czf /backup/jobs_backup_$(date +%Y%m%d).tar.gz -C /data .
```

اگر دیسک سرور محدود است، توصیه می‌شود یک کرون‌جاب برای پاک‌سازی
job-های قدیمی‌تر از N روز اضافه کنید (فایل‌های صوتی/ویدیو حجیم‌اند).

---

## ۱۰) تمدید خودکار گواهی SSL

سرویس `certbot` در `docker-compose.yml` به‌صورت خودکار هر ۱۲ ساعت چک
می‌کند و گواهی را در صورت نزدیک‌شدن به انقضا (کمتر از ۳۰ روز) تمدید
می‌کند. کار دستی اضافه‌ای لازم نیست، فقط مطمئن شوید که کانتینر
`quran-analyzer-certbot` همیشه در حال اجراست (`docker compose ps`).

---

## نکات امنیتی مهم (پیش از این‌که پروژه واقعاً عمومی شود)

این‌ها **فعلاً در کد پیاده‌سازی نشده‌اند** — بسته به این‌که چقدر ترافیک
عمومی و ناشناس انتظار دارید، پیشنهاد می‌کنم قبل از تبلیغ گسترده به آن‌ها
فکر کنید و اگر خواستید اضافه‌شان کنم:

1. **محدودیت نرخ درخواست (Rate limiting)**: در حال حاضر هرکسی می‌تواند
   بدون محدودیت فایل آپلود/تحلیل/مقایسه کند. یک کاربر مخرب می‌تواند با
   آپلود پیاپی فایل‌های طولانی، CPU سرور را قفل کند (DoS ساده). راه‌حل:
   افزودن rate-limit در سطح nginx (`limit_req`) یا در FastAPI.
2. **محدودیت حجم/مدت فایل ورودی**: در حال حاضر `client_max_body_size
   200M` در nginx تنظیم شده، اما در خود اپ محدودیتی روی مدت زمان فایل
   صوتی اعمال نشده — یک فایل ۲ ساعته می‌تواند دقیقه‌ها CPU سرور را درگیر
   کند.
3. **پاک‌سازی خودکار job های قدیمی**: پوشهٔ `server/jobs/` فقط رشد
   می‌کند؛ اگر ترافیک واقعی داشته باشید، دیسک سرور تمام می‌شود. نیاز به
   یک job پاک‌سازی دوره‌ای (مثلاً حذف فایل‌های قدیمی‌تر از ۷ روز) دارد.
4. **CORS باز (`allow_origins=["*"]`)**: فعلاً هر دامنه‌ای می‌تواند به
   API شما درخواست AJAX بزند. اگر فقط قرار است از طریق خودِ همین دامنه
   استفاده شود، بهتر است این را محدود کنید.

اگر بخواهید، در یک نوبت جداگانه این چهار مورد را هم پیاده‌سازی می‌کنم —
فقط بگویید کدام‌ها اولویت دارند.
